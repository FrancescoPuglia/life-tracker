/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  images: {
    unoptimized: true
  },
  basePath: '/life-tracker',
  assetPrefix: '/life-tracker/'
}

module.exports = nextConfig